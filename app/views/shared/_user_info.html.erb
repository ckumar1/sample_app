<%= link_to gravatar_for(current_user, size: 52), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "view my profile", current_user, {:style=>'color:#FF0000;'} %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "micromessage") %>
</span>